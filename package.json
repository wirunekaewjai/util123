{
  "name": "util123",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:css": "tailwindcss -i ./tailwind.css -o ./assets/style.css --watch",
    "dev:rs": "cargo watch -w src/server -x 'run'",
    "dev:ts": "nodemon --watch src/client -e ts,tsx --exec 'bun build src/client/app.ts --outdir assets --splitting'",
    "dev": "rimraf assets && concurrently 'bun run dev:rs' 'bun run dev:ts' 'bun run dev:css'",
    "deploy": "bun run build && fly deploy",
    "build:css": "tailwindcss -i ./tailwind.css -o ./assets/style.css --minify",
    "build:ts": "bun build src/client/app.ts --outdir assets --minify --splitting",
    "build": "rimraf assets && bun run build:css && bun run build:ts",
    "start": "cargo run --release"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/qrcode": "^1.5.5",
    "@wirunekaewjai/jetpack": "github:wirunekaewjai/jetpack#da900bb43a460d1b6072e0feabe39d2440b2e842",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.2",
    "rimraf": "^5.0.7",
    "tailwindcss": "^3.4.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  }
}